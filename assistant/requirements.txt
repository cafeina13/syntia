# Extra packages for the voice assistant (assistant/). The music bot does not need these.
-r ../requirements.txt
faster-whisper>=1.1
piper-tts>=1.3
openwakeword>=0.6
# GPU speech-to-text on Windows: cuBLAS for CTranslate2 (~900 MB). Skip it with STT_DEVICE=cpu.
nvidia-cublas-cu12; sys_platform == "win32"
