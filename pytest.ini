[pytest]
testpaths = tests
# Lets tests `import music` etc. straight from the project root.
pythonpath = .
# Plain `async def test_...` functions just work, no decorator needed.
asyncio_mode = auto
markers =
    ffmpeg: needs FFmpeg on PATH (skipped automatically when it's missing)
