# Secrets — never commit
.env
.spotify_cache
.cache

# Python virtual environment
.venv/
__pycache__/
*.pyc

# Local-only helper (one-time cleanup script, kept for reference)
clear_global.py

# Voice assistant spikes: raw recordings of people talking (never commit)
recordings/
